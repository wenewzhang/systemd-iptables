[Unit]
Description=White List on nft
Documentation=
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/sbin/nft -f /usr/bin/whitelist.nft
ExecReload=/usr/sbin/nft -f  /usr/bin/whitelist.nft
ExecStop=/usr/sbin/nft flush ruleset
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
